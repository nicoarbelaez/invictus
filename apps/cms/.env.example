############################
# Strapi Configuration
############################
# See https://docs.strapi.io/cms/configurations/environment

# Node environment (development, staging, production)
NODE_ENV=development

# Server
HOST=0.0.0.0
PORT=1337

############################
# Database
############################
# PostgreSQL (default):
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi123
DATABASE_SSL=false

# For SQLite (development without dependencies, uncomment):
# DATABASE_CLIENT=sqlite
# DATABASE_FILENAME=.tmp/data.db

############################
# Authentication (CHANGE ALL IN PRODUCTION)
############################
# Generate each key: node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"
# APP_KEYS: generate 4 separate keys, join with commas
APP_KEYS=toBeModified1,toBeModified2,toBeModified3,toBeModified4 # CHANGE IN PRODUCTION
API_TOKEN_SALT=toBeModified # CHANGE IN PRODUCTION
ADMIN_JWT_SECRET=toBeModified # CHANGE IN PRODUCTION
TRANSFER_TOKEN_SALT=toBeModified # CHANGE IN PRODUCTION
JWT_SECRET=toBeModified # CHANGE IN PRODUCTION
ENCRYPTION_KEY=toBeModified # CHANGE IN PRODUCTION

CLOUDINARY_NAME=cloud_name
CLOUDINARY_KEY=api_key
CLOUDINARY_SECRET=api_secret

############################
# Deploy provider trigger
############################
# Active provider — valid values: dokploy | vercel
DEPLOY_PROVIDER=dokploy
# Minimum time between redeploys in ms (default: 60000 = 1 min)
DEPLOY_COOLDOWN_MS=60000

# Dokploy
DOKPLOY_REDEPLOY_URL=http://<host>:<port>/api/compose.redeploy
DOKPLOY_COMPOSE_ID=<compose-id>
DOKPLOY_TITLE=Strapi - Invictus CMS Production Deploy
DOKPLOY_DESCRIPTION=Deploy automático desde Strapi (CMS) para el proyecto Invictus en entorno production. Trigger de publicación de contenido.
DOKPLOY_API_KEY=<api-key>

# Vercel
VERCEL_TOKEN=<vercel-token>
VERCEL_PROJECT_ID=<project-id>